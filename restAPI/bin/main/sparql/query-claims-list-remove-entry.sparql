prefix auicql: <https://www.caci.com/aida-ui/claimquerylist#>

DELETE
 { ?queryClaimList ?o ?assocaitedClaimQueryId }
WHERE
  {
    ?queryClaimList ?o ?p ;
    auicql:queryListId ?queryListId ;
    auicql:assocaitedClaimQueryId ?assocaitedClaimQueryId ;
 	  FILTER ( ?queryListId = "{$queryListId}"^^xsd:string && ?assocaitedClaimQueryId = "{$queryId}"^^xsd:string)
  };